#include <iostream>
#include <thread>
#include <sys/socket.h>
class read_in{
    ifstream file;
    char* filename;
    file.open(filename, ios::binary | ios::in))
    //file.reAD(&)
int front = socket(domain, type, protocol)
std::thread obj()
}